import { APP_INITIALIZER } from '@angular/core';

/**
 * Proveedores para la configuración de la aplicación
 */
export const appProviders = [
  // En la arquitectura simplificada, los servicios se auto-configuran
  // o se inicializan a través de AppConfigurationService
];
